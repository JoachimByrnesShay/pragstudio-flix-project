<section class="movie" style="height: 1000px;">
<h1> Edit Genres </h1>
<section class = "admin" style="position: absolute; display: inline-block; left: 7%" >
<ul>
<% @genres.each do |g| %>
 <div > <%= link_to g.name, genre_path(g) %> </div> 
  <p> 
    <%=link_to "Edit", '', class: "button delete" %> <%= link_to "Delete", '', data: {confirm: "Are you sure you want to permanently delete this genre?"}, class: "button delete" %> 
  </p>
</li>
<% end %>
</ul>
</section>
</section>